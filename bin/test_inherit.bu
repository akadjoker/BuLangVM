class A {
    def foo() {
        print("A.foo");
    }
}

class B : A {
    // NÃO override foo()
}

class C : B {
    def bar() {
        print("Calling super.foo from C");
        super.foo();  // O que acontece aqui?
    }
}

var b = B();
b.foo();  // Funciona?

var c = C();
c.bar();  // Funciona?
