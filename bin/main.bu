// ===== TESTE WARNING: Variáveis globais com nomes de privates =====

// Estas variáveis devem gerar warnings porque x, y, z são privates de processo
var x = 10;
var y = 20;

print("x=" + x + " y=" + y);

// Processo para demonstrar
process TestProcess() {
    // x e y aqui vão usar os GLOBAIS, não as privates!
    print("Dentro do processo: x=" + x + " y=" + y);
    loop { frame; }
}

var p = TestProcess();
print("Processo criado");

print("FIM");
