smoothstep()
============

Smooth hermite interpolation.

.. code-block:: bulang

   // Normalize t (0..1)
   smooth = math.smoothstep(t);

   // With custom range
   smooth = math.smoothstep(0.2, 0.8, x);
