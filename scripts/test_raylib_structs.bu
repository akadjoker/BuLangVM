import raylib;
using raylib;

// Teste mínimo de structs nativos do raylib
var c = Color(255, 128, 64, 200);
print("Color ok? ", c.r, c.g, c.b, c.a);

var v = Vector2(1.5, 2.5);
print("Vector2 ok? ", v.x, v.y);

var r = Rectangle(10.0, 20.0, 30.0, 40.0);
print("Rectangle ok? ", r.x, r.y, r.width, r.height);

// Garantir que não existe colisão com builtins
print("int(42) = ", int(42));
print("real(3) = ", real(3));
