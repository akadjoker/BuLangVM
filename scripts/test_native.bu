// Testa se conseguimos aceder a classes nativas (que devem estar registadas como globais)

// Testa array nativo (já existe)
var arr = [];
arr.push(10);
arr.push(20);
arr.push(30);

print("Array len: " + str(len(arr)));
print("Array[0]: " + str(arr[0]));
print("Array[1]: " + str(arr[1]));

// Testa criação de múltiplos arrays
var arr2 = [];
var arr3 = [];
arr2.push(100);
arr3.push(200);

print("arr2[0]: " + str(arr2[0]));
print("arr3[0]: " + str(arr3[0]));

// Testa string nativa
var s = "Hello";
print(s);

print("TUDO OK!");
