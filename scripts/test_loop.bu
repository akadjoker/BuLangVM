def worker() { print("worker"); }

process test() {
    var i = 0;
    while (i < 5) {
        fiber worker();  // Fiber dentro de loop!
        i++;
    }
    var j = 0;
    while (j < 20) { frame; j++; }
}

test();
